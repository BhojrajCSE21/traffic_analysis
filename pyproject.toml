[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "traffic-analytics-api"
version = "1.0.0"
description = "Traffic Analytics Platform API"
requires-python = ">=3.9"
